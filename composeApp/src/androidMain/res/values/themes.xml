<resources>
    <!-- 🌌 Main App Theme -->
    <style name="Theme.SPICA" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/black</item>
    </style>

    <!-- 🌅 Splash Theme (shows layout instantly) -->
    <style name="Theme.SPICA.Splash" parent="Theme.SPICA">
        <item name="android:windowBackground">@color/black</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>
