<resources>

    <!-- 🌑 Main App Theme -->
    <style name="Theme.SPICA" parent="Theme.Material3.NoActionBar">
        <item name="android:windowBackground">@color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>

    <!-- 🚀 Native Splash Theme -->
    <style name="Theme.SPICA.Splash" parent="Theme.SplashScreen">
        <!-- Background color -->
        <item name="windowSplashScreenBackground">@color/black</item>

        <!-- Logo (centered icon or image) -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher</item>

        <!-- Optional logo circle background -->
        <item name="windowSplashScreenIconBackgroundColor">@android:color/transparent</item>

        <!-- After splash, switch to this -->
        <item name="postSplashScreenTheme">@style/Theme.SPICA</item>
    </style>

</resources>
