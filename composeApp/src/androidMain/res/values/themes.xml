<resources>

    <!-- 🌙 Main App Theme -->
    <style name="Theme.SPICA" parent="@android:style/Theme.Material3.Dark.NoActionBar">
        <item name="android:windowBackground">@color/black</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/black</item>
    </style>

    <!-- 🚀 Splash Theme (used before Compose starts) -->
    <style name="Theme.SPICA.Splash" parent="Theme.SPICA">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@null</item>
        <item name="android:windowSplashScreenBackground">@null</item>
    </style>

</resources>
